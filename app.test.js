const request = require("supertest");
// const app = require('./index');

// TODO Add more test. Not enough time.
describe('REST API', () => {
    it('GET /api/truck', () => {
        // return request(app).get('/api/truck').expect(200)
    })
})
